# AI-CONTEXT:
#
# Purpose:
# - Cloudflare Worker configuration for treishvaamagro-seo-worker.
#
# Scope:
# - Defines KV bindings, environmental variables mappings, and compatibility dates.
#
# Critical Dependencies:
# - Backend: finance-api (Configured dynamically via secrets, not hardcoded here).
# - Frontend: Agro Pages deployment.
# - Worker / SEO / Sitemap: TREISHFIN_SEO_CACHE KV namespace.
#
# Security Constraints:
# - NEVER hardcode production secrets (BACKEND_ORIGIN, CF_PAGES_ORIGIN) here. 
# - Use `wrangler secret put` or the Cloudflare Dashboard strictly.
#
# Non-Negotiables:
# - Must retain the exact KV namespace binding "TREISHFIN_SEO_CACHE" to utilize the shared free-tier cache.
#
# Change Intent:
# - Phase 1 configuration for Agro SEO Edge Worker.
#
# Future AI Guidance:
# - Do not add inline secrets.
# - Do not change the KV binding name.
#
# IMMUTABLE CHANGE HISTORY (DO NOT DELETE):
# - ADDED:
# • Initial wrangler.toml for treishvaamagro-seo-worker.
# • Mapped TREISHFIN_SEO_CACHE KV namespace to inherit established SEO caching.
# • Phase 1 (Agro Edge Architecture).
#
# - DO-NOT-DELETE RULE:
# This IMMUTABLE CHANGE HISTORY section must never be deleted,
# truncated, rewritten, or regenerated.
# Future AI must append only.

name = "treishvaamagro-seo-worker"
main = "worker.js"
compatibility_date = "2024-03-20"

# Note: Production values for CF_PAGES_ORIGIN and BACKEND_ORIGIN MUST be set via Cloudflare Secrets.

[[kv_namespaces]]
binding = "TREISHFIN_SEO_CACHE"
id = "035cdbfcc43048999ea6e8f47c32cf97"